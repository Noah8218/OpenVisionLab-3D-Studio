<UserControl x:Class="OpenVisionLab.ThreeD.Shell.Views.Workbench.LandmarkCorrespondenceEvidenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:i18n="clr-namespace:OpenVisionLab.ThreeD.Shell"
             AutomationProperties.AutomationId="LandmarkCorrespondenceEvidenceView">
    <Grid Margin="12,8" Background="{DynamicResource ThreeD.PanelBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{DynamicResource ThreeD.DockPanelTitleTextStyle}"
                           Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.Title, Korean='랜드마크 대응 증거', English='Landmark Correspondence Evidence'}" />
                <TextBlock Style="{DynamicResource ThreeD.DockPanelCaptionTextStyle}"
                           Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.Caption, Korean='Published CornerAnchor 입력과 명시적 기준 좌표 대응입니다.', English='Published CornerAnchor inputs and explicit reference-coordinate correspondence.'}" />
            </StackPanel>
            <Border Grid.Column="1"
                    Style="{DynamicResource ThreeD.DockPanelStatusCardStyle}"
                    ToolTip="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.ReadOnlyTip, Korean='이 창은 증거만 표시하며 어파인 변환을 계산하지 않습니다.', English='This pane reports evidence only; it never calculates the affine transform.'}"
                    AutomationProperties.Name="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.ReadOnlyAction, Korean='읽기 전용 랜드마크 대응 증거', English='Read-only landmark correspondence evidence'}">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Width="13" Height="13" Margin="0,0,4,0" Foreground="{DynamicResource ThreeD.MutedTextBrush}" Symbol="Document24" />
                    <TextBlock FontSize="10"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ThreeD.MutedTextBrush}"
                               Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.ReadOnly, Korean='읽기 전용', English='Read-only'}" />
                </StackPanel>
            </Border>
        </Grid>
        <Border Grid.Row="1"
                Margin="0,8,0,6"
                Padding="10,8"
                Background="{DynamicResource ThreeD.PanelAlternateBrush}"
                BorderBrush="{DynamicResource ThreeD.DividerBrush}"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock FontSize="10"
                               Foreground="{DynamicResource ThreeD.MutedTextBrush}"
                               Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.PublishedInputs, Korean='Published 입력', English='Published inputs'}" />
                    <TextBlock Margin="0,2,0,0" FontSize="10.5" Foreground="{DynamicResource ThreeD.SecondaryTextBrush}" TextWrapping="Wrap" Text="{Binding LandmarkCorrespondenceUpstreamSummary}" />
                    <TextBlock Margin="0,8,0,0"
                               FontSize="10"
                               Foreground="{DynamicResource ThreeD.MutedTextBrush}"
                               Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.CurrentState, Korean='현재 대응 상태', English='Current correspondence state'}" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource ThreeD.PrimaryTextBrush}" TextWrapping="Wrap" Text="{Binding LandmarkCorrespondenceEvidenceSummary}" />
                    <TextBlock Margin="0,8,0,0" FontSize="9.5" Foreground="{DynamicResource ThreeD.MutedTextBrush}" TextWrapping="Wrap" Text="{Binding LandmarkCorrespondenceOutputHashSummary}" />
                </StackPanel>
            </ScrollViewer>
        </Border>
        <TextBlock Grid.Row="2"
                   FontSize="9.5"
                   Foreground="{DynamicResource ThreeD.MutedTextBrush}"
                   TextWrapping="Wrap"
                   Text="{i18n:ThreeDText Key=ThreeD.Workbench.CorrespondenceEvidence.Guidance, Korean='현재 Published CornerAnchor 4개와 명시적 기준 좌표 4개가 비퇴화 사면체를 이루어야 합니다. 이 증거는 어파인 행렬을 계산하거나 보정, 계측, OK/NG를 주장하지 않습니다.', English='Exactly four current Published CornerAnchors and four explicit reference coordinates must form non-degenerate tetrahedra. This evidence does not calculate an affine matrix or claim calibration, metrology, or OK/NG.'}" />
    </Grid>
</UserControl>
